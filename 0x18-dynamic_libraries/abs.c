#include "main.h"

/**
* _abs - Starts tocompute the absolute value of an integer
* @n: Specifies the int to check
* Return: The absolute value (Ends)
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
