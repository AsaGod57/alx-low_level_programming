#include "main.h"

/**
* _islower - Start to check for lowercase letters
* @c: Specifies character to check
* Return: 1 if c is lowercase, 0 if otherwise (Ends)
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
