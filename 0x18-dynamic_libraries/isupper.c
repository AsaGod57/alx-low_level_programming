#include "main.h"

/**
 * _isupper - Starts to check for uppercase characters
 * @c: Specifies character to check
 * Return: 1 if c is uppercase or 0 if otherwise
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
