#include "main.h"

/**
 * _isdigit - Starts to check for a digit 0 through 9
 * @c: Specifies digit to check
 * Return: 1 if c is a digit or 0 if otherwise (Ends)
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
