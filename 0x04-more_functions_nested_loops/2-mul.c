#include "main.h"

/**
  * mul - Start point
  * @x: Specifies the first number to multiply
  * @y: Specifies the second number to multiply with first number
  *
  * Return: The result of multiplication between x param and y param (Ends)
  */
int mul(int x, int y)
{
	return (x * y);
}
