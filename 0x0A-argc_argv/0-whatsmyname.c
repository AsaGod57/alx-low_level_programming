#include "main.h"
#include <stdio.h>

/**
 * main - Start point
 * @argc: Specifies the argument count
 * @argv: Specifies the array of argument strings
 * Return: 0 for succesful exit (Ends)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
