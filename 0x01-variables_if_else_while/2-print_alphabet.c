#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Start point
 *
 * Return: Always 0 (End)
 */

int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
		putchar('\n');

	return (0);
}
