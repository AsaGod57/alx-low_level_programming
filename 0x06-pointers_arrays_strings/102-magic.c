#include "main.h"
#include <stdio.h>
/**
 * _magic - Start point
 * @n - Specifies the value
 * Return: Void (Ends)
 */
int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &n;

	printf("a[2] = %d\n", a[2]);
	return (0);
}
